/**
 * Modèle de configuration locale.
 *
 * 1. Copiez ce fichier en `src/js/config.js`
 * 2. Collez votre clé API TMDB (v3 auth) ci-dessous
 *
 * `src/js/config.js` est ignoré par git : votre clé ne partira jamais sur GitHub.
 *
 * Ce fichier est optionnel : sans lui, l'application vous demandera la clé
 * au premier lancement et la stockera dans le localStorage du navigateur.
 */
export const TMDB_API_KEY = '';
